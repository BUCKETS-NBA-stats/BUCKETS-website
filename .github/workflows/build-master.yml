name: Update mappings + combined league table

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "assets/data/season/**"
      - "mappings/name_fixer_inputs/**"
      - "scripts/build_player_aliases.py"
      - "scripts/build_master.py"
      - ".github/workflows/build-master.yml"
      - "requirements.txt"

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Rebuild player mappings
        run: |
          python scripts/build_player_aliases.py

      - name: Rebuild combined league table
        run: |
          python scripts/build_master.py --season-dir "assets/data/season" --output "assets/data/league-table-combined.csv"

      - name: Commit updated outputs (if changed)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add mappings/players.csv mappings/player_aliases.csv assets/data/league-table-combined.csv

          git diff --cached --quiet || git commit -m "Auto-update mappings and combined league table"
          git push
